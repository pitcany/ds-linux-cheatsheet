[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ds-linux-cheatsheet"
version = "0.1.0"
description = "Interactive TUI cheat sheet of Linux commands for data scientists and ML engineers."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Yannik" }]
keywords = ["tui", "cheatsheet", "linux", "datascience", "ml", "textual"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console :: Curses",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Utilities",
]
dependencies = [
    "textual>=0.79.0",
    "rich>=13.7.0",
    "pydantic>=2.6.0",
    "pyyaml>=6.0.1",
    "pyperclip>=1.8.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.4.0",
]

[project.scripts]
ds-cheatsheet = "ds_cheatsheet.cli:main"

[project.urls]
Homepage = "https://github.com/pitcany/ds-linux-cheatsheet"

[tool.hatch.build.targets.wheel]
packages = ["src/ds_cheatsheet"]

[tool.hatch.build.targets.wheel.shared-data]
"data/commands" = "ds_cheatsheet/data/commands"

[tool.hatch.build]
include = [
    "src/ds_cheatsheet/**",
    "data/commands/*.yaml",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N", "RUF"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-ra -q"
asyncio_mode = "auto"
